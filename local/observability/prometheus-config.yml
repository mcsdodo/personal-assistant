# Minimal Prometheus config for local dev
# Only receives remote_write from Alloy — no scrape targets needed
global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: email-watcher
    static_configs:
      - targets: ["claude-code:9465"]
